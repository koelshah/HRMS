import { e } from './page-header.module';

describe('e', () => {
    let e: e;

    beforeEach(() => {
        e = new e();
    });

    it('should create an instance', () => {
        expect(e).toBeTruthy();
    });
});
